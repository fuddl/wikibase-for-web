<a class="thing" href="{{ href }}" data-id="{{ globalId }}" title="{{ description | escape('html_attr') }}" {% if label.value %}data-complete="true"{% endif %}>
	{{- label.value ?? globalId | e -}}
</a>