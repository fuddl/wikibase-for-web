<picture class="pic {% if scaleable %}pic--scaleable{% endif %}">
	{% for source in sources %}
		<source srcset="{{ source.srcSet }}" />
	{% endfor %}
	<img class="pic__placeholder" src="{{ src }}" loading="lazy"/>
</picture>